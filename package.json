{
  "name": "kemitchell-payments",
  "description": "online credit card payment portal",
  "version": "0.0.0",
  "dependencies": {
    "concat-stream": "^1.4.8",
    "routes": "^2.0.0",
    "stream-replace": "^1.0.0",
    "stripe": "^3.3.4"
  },
  "devDependencies": {
    "chai": "^2.2.0",
    "clone": "^1.0.2",
    "istanbul": "^0.3.13",
    "jscs": "^1.12.0",
    "jshint": "^2.7.0",
    "mocha": "^2.2.4",
    "object-assign": "^2.0.0"
  },
  "engines": {
    "iojs": "1.7.x",
    "npm": "2.8.x"
  },
  "main": "server/handler",
  "private": true,
  "scripts": {
    "lint": "jshint client server test && jscs client server test",
    "pre-commit": "npm run lint && npm run test",
    "start": "node server/serve.js",
    "test": "mocha"
  }
}
